{
  "name": "reader",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "prettier --write '**/*.js'",
    "clean": "rm -rf ./dist/*",
    "dev": "parcel --no-minify src/**/*.{html,js,css}",
    "prod": "parcel build --no-source-maps src/**/*.{html,js,css}",
    "copy": "cp -RT ./src/static ./dist",
    "build": "run-s prod copy",
    "build-dev": "run-s dev copy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andreagualandi/reader.git"
  },
  "author": "",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/andreagualandi/reader/issues"
  },
  "homepage": "https://github.com/andreagualandi/reader#readme",
  "browserslist": [
    "last 1 Chrome version"
  ],
  "devDependencies": {
    "eslint": "^7.5.0",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.4.3",
    "prettier": "^2.0.5"
  },
  "dependencies": {}
}