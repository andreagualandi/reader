{
  "name": "reader",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/content.js src/options.html",
    "build-prod": "parcel build src/content.js src/options.html",
    "build": "parcel build --no-source-maps --no-minify src/**/*.html src/**/*.css src/content.js src/background.js --public-url ./",
    "build:cpx": "cpx 'src/**/*.{json,png,svg}' dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andreagualandi/reader.git"
  },
  "author": "",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/andreagualandi/reader/issues"
  },
  "homepage": "https://github.com/andreagualandi/reader#readme",
  "browserslist": [
    "last 1 Chrome version"
  ],
  "devDependencies": {
    "cpx": "^1.5.0",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.4.3"
  }
}